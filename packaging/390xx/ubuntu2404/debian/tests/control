# autopkgtest — runs in a throwaway container/VM with NO NVIDIA GPU present.
# The full harness lives in ../../tests/ of the flavour repo; these are the
# in-archive smoke tests.

Test-Command: /usr/share/nvidia-legacy-tests/install-purge.sh 390xx
Depends: nvidia-legacy-390xx-driver, nvidia-legacy-tests
Restrictions: needs-root, allow-stderr, isolation-container

Test-Command: /usr/share/nvidia-legacy-tests/file-conflicts.sh 390xx
Depends: nvidia-legacy-390xx-driver, nvidia-legacy-tests
Restrictions: needs-root, allow-stderr, isolation-container

Test-Command: /usr/share/nvidia-legacy-tests/dkms-build.sh 390xx
Depends: nvidia-legacy-390xx-kernel-dkms, nvidia-legacy-tests,
 linux-headers-generic | linux-headers-amd64
Restrictions: needs-root, allow-stderr, isolation-machine, skippable

Test-Command: /usr/share/nvidia-legacy-tests/xorg-dummy.sh 390xx
Depends: nvidia-legacy-390xx-driver, nvidia-legacy-tests, xserver-xorg-core, xvfb
Restrictions: needs-root, allow-stderr, isolation-container
